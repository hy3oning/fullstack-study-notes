	switch (정수,문자,문자열 객체) {
	case 위치값1: 객체=위치값 1 이면 이 위치로 이동
		실행내용;
		break;
	case 위치값2: 객체=위치값 2 이면 이 위치로 이동
		실행내용;
		break;
	case 위치값3: 객체=위치값 3 이면 이 위치로 이동
		실행내용;
		break;
	default: 일치하는 위치값이 없는 경우 이 위치로 이동
		실행내용;
	}

콜론(:)의 개념적 의미
- java프로그램에서 콜론은 이정표의역할
- 삼항연산자, case, 레이블 및 람다식
- switch문 탈출을 위해서는 break키워드 사용