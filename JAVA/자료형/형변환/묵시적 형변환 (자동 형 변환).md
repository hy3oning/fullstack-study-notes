
값이 서로 안맞을 때에는 컴파일러가 자동으로 맞춰준다 
단, 값을 잃어버리지 않는 선에서.

ex ) 
	double a = 5; //5.0


자동 형 변환의 원칙은 표현범위가 좁은 [[데이터 타입]]에서 넓은 [[데이터 타입]]으로의 변환만 허용된다.


byte ➡️short,char➡️int➡️long➡️float➡️double



short와 byte로 연산을 진행할때에는 정수형(int)로 묵시적 형변환이 되어 계산된다.