
참(true), 거짓(false) 값만 저장하는 자료형
boolean 의 기본값 = false;

ex)
	boolean a = true;
	boolean b = false;


boolean  자료형은 참과 거짓으로만 저장하여 실제로는 1bit로 가능

하지만 자료처리의 최소위가 byte이기 때문에 1byte를 할당
상위 7bit는 사용하지 않음

[[데이터 타입]]은 1byte