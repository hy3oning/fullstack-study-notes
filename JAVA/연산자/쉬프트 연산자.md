	왼쪽 쉬프트 ( << )
	int x = 3;   // 0011
	System.out.println(x << 2); // 12 (1100)
	
	오른쪽 쉬프트 ( >> )
	int y = 8;   // 1000
	System.out.println(y >> 1); // 4 (0100)
	-부호비트를 유지 (음수면 1로 채움)
	
	부호 없는 오른쪽 쉬프트 ( >>> )
	int z = -8;
	System.out.println(z >>> 1);
	-왼쪽을 무조건 0으로 채움 주로 비트 단위 계산에서 사용
