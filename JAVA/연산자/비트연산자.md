	AND (&)
	int a = 5;   // 0101
	int b = 3;   // 0011
	System.out.println(a & b); // 1 (0001)
	
	OR ( | )
	System.out.println(a | b); // 7 (0111)
	
	XOR( ^ )
	System.out.println(a ^ b); // 6 (0110)
	
	NOT ( ~ )
	System.out.println(~a); 
	// 0101 → 1010 (2의 보수 체계로 인해 -6)
