
⭐멤버 및 생성자의 접근 지정자.

-  ==4가지 접근지정자 ( public, protected, default, private)

		public = 동일 패키지의 모든 클래스 + 다른 패키지의 모든 클래스 사용가능
		protected = 동일 패키지의 모든 클래스 + 다른 패키지의 상속클래스 사용가능
		default = 동일 패키지의 모든 클래스에서 사용가능
		private = 동일 클래스에서 사용가능

⭐클래스의 접근 지정자.
- ==2가지 접근지정자 ( public, default)

